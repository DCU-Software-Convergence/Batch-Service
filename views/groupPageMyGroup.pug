br
br
div(class="table-responsive")
    table(class="table table-bordered")
        thead
            tr
                th 그룹 이름
                th 그룹장
                th 멤버
        tbody
            - var group_len = myGroup.length;
            - for (var i=group_len-1;i>=0;i--) {
                - var groupURL = "group/grouplist/"+myGroup[i].groupUrlName;
                - var userURL = "user/" + myGroup[i].groupLeader;
                tr
                    th
                        a(class="groupText" href=groupURL) #{myGroup[i].groupName}
                    th 
                        a(class="groupText" href=userURL) #{myGroup[i].groupLeader}
                    th #{myGroup[i].groupMember.length}
            -}